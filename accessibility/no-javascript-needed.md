# No JavaScript Needed

This archive does not require JavaScript.

It does not require a build step.

It does not require a package manager.

A browser, text editor, terminal pager, or printed copy is enough.
