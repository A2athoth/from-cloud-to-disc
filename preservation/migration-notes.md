# Migration Notes

If this archive must be moved to another medium:

1. Preserve the directory structure.
2. Preserve UTF-8 text encoding.
3. Regenerate checksums after intentional changes.
4. Keep Korean filenames and text intact.
5. Avoid converting Markdown to a format that cannot be diffed.

Migration should make the archive easier to read, not harder to inspect.
